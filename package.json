{
  "name": "bbai-alt-text-plugin",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test": "jest"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "jest": "^29.0.0"
  }
}
