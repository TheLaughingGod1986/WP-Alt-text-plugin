# Git exclude patterns (this file is not named ".gitignore" so WordPress Plugin Check does not flag a hidden file).
# After clone, run once: ./scripts/bootstrap-git-excludes.bash
.DS_Store
**/.DS_Store
# macOS AppleDouble / resource fork metadata (never commit)
**/._*
/build/
.playwright-cli/
output/

# e2e tooling (dependencies installed locally)
tests/e2e/node_modules/
tests/e2e/playwright-report/
tests/e2e/test-results/
